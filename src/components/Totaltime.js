import React from 'react'

const Totaltime = (props) => {
    const setTime =()=>{
        return (props.item)
    }
    
    return (        
            <h4>{setTime()}</h4>
    )
}



export default Totaltime
